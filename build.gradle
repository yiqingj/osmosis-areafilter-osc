dependencies {
    compile project(':osmosis-core')
    compile project(':osmosis-replication')
    compile project(':osmosis-areafilter')
    testCompile project(':osmosis-xml')
    testCompile project(':osmosis-testutil')
}
